{
  "name": "Job Search React",
  "description": "A description of my app",
  "scripts": {
    "postdeploy": "npm run dev"
  },
  "env": {
    "NODE_ENV": "development"
  },
  "environments": {
    "test": {
      "scripts": {
        "test-setup": "pip install -r requirements.txt",
        "test": "pytest"
      },
      "formation": {
        "test": {
          "quantity": 1,
          "size": "standard-2x"
        }
      }
    }
  }
}
